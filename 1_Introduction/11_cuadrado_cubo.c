/*
	Juan Victor 
*/

#include <stdio.h>

int main(void)
{
	/* Declaracion de variables */
	int numero = 0;

	printf("numero\tcuadrado\tcubo\n");
	printf("%d\t%d\t\t%d\n",numero, numero*numero, numero*numero*numero );
	numero = numero + 1;

	printf("%d\t%d\t\t%d\n",numero, numero*numero, numero*numero*numero );
	numero = numero + 1;

	printf("%d\t%d\t\t%d\n",numero, numero*numero, numero*numero*numero );
	numero = numero + 1;

	printf("%d\t%d\t\t%d\n",numero, numero*numero, numero*numero*numero );
	numero = numero + 1;

	printf("%d\t%d\t\t%d\n",numero, numero*numero, numero*numero*numero );
	numero = numero + 1;

	printf("%d\t%d\t\t%d\n",numero, numero*numero, numero*numero*numero );
	numero = numero + 1;

	printf("%d\t%d\t\t%d\n",numero, numero*numero, numero*numero*numero );
	numero = numero + 1;

	printf("%d\t%d\t\t%d\n",numero, numero*numero, numero*numero*numero );
	numero = numero + 1;

	printf("%d\t%d\t\t%d\n",numero, numero*numero, numero*numero*numero );
	numero = numero + 1;

	printf("%d\t%d\t\t%d\n",numero, numero*numero, numero*numero*numero );
	numero = numero + 1;





	/*FIN */
	return 0;
}